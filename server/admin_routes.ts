import type { Express } from "express";

export async function registerAdminRoutes(app: Express): Promise<void> {
  // Admin routes are now integrated into main routes.ts
  // This file is kept for backwards compatibility
}
