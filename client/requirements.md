## Packages
framer-motion | Smooth animations for page transitions and workout items
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely
lucide-react | Icon set
date-fns | Date formatting

## Notes
- Backend uses Replit Auth, need to handle 401 redirects.
- Using Google Fonts: 'Outfit' (Display) and 'DM Sans' (Body).
- Theming needs to handle dynamic CSS variables for gym brands.
